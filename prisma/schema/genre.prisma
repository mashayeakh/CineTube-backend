model Genre {
    id                 String              @id @default(cuid())
    name               String              @unique
    movies             Movie[]             @relation("MovieGenres")
    movieContributions MovieContribution[] @relation("MovieContributionGenres")

    @@map("genre")
}
