enum PriceType {
  FREE
  PREMIUM
}

enum UserStatus {
  ACTIVE
  ARCHIVED
  BLOCKED
  DELETED
}

enum MovieStatus {
  PENDING
  APPROVED
  REJECTED
}

enum ReviewStatus {
  PENDING
  APPROVED
  REJECTED
}

enum PaymentStatus {
  PENDING
  COMPLETED
  FAILED
}

enum SubscriptionType {
  MONTHLY
  YEARLY
  TRIAL
}

enum SubscriptionStatus {
  PENDING
  ACTIVE
  EXPIRED
  CANCELLED
}

enum UserRole {
  USER
  ADMIN
  PREMIUM_USER
  GUEST
}

// For chat messages, we can have roles like USER and BOT to differentiate between user messages and bot responses.q
enum ChatRole {
  USER
  BOT
  GUEST
}

enum AgeGroup {
  AGE_18_PLUS
  AGE_13_PLUS
  ALL_AGES
}

enum SeriesStatus {
  ONGOING
  COMPLETED
  UPCOMING
  CANCELLED
}

enum SeriesTrackingStatus {
  PLAN_TO_WATCH
  WATCHING
  ON_HOLD
  COMPLETED
  DROPPED
}
