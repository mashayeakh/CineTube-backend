model ChatMessage {
    id String @id @default(uuid())

    userId String?
    user   User?   @relation(fields: [userId], references: [id])

    role    ChatRole
    message String
    result  String? // AI response stored here

    createdAt DateTime @default(now())

    @@index([userId])
    @@map("chat_messages")
}
